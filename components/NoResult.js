const NoResult = ({message}) => {
    return (
        <main className="pt-[10%] flex flex-col">
            <h1 className="text-3xl text-silver my-auto text-center">{message}</h1>
        </main>
    )
}

export default NoResult;